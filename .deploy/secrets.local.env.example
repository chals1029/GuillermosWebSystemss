# Copy to secrets.local.env (gitignored). Used by deploy scripts to avoid
# putting credentials in git history.
VPS_HOST=20.189.74.35
VPS_USER=BeaBunda
# The VPS sudo / login password. Never commit this file.
VPS_PASSWORD=changeme
